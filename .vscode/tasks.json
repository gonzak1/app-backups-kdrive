{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "publish (functions)",
      "type": "process",
      "command": "dotnet",
      "args": [
        "publish",
        "${workspaceFolder}/app-backups-kdrive.csproj",
        "--configuration", "Release",
        "--framework", "net8.0",
        "--output", "${workspaceFolder}/bin/Release/net8.0/publish"
      ],
      "problemMatcher": "$msCompile"
    }
  ]
}
